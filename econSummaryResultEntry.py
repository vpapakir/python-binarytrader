class econSummaryResultEntry:
	caseID = 0
	weekendTimeout = 0
	maxSpread = 0
	activityCriterion = 0 
	invertEMA = 0
	buyRSI = 0
	buyEMADist = 0  
	sellRSI = 0
	sellEMADist = 0 
	takeProfit = 0
	stopLoss = 0
	buysOpened = 0
	buysTakeProfit = 0
	buysStopLoss = 0
	buysClosed = 0
	buyPoints = 0
	sellsOpened = 0
	sellsTakeProfit = 0
	sellsStopLoss = 0	
	sellsClosed = 0 
	sellPoints  = 0
	totalTrades = 0 
	totalWins   = 0
	totalLosses = 0
	totalLosses = 0
	totalPoints = 0

	def __init__(self,caseID, weekendTimeout, maxSpread, activityCriterion, invertEMA, buyRSI, buyEMADist, sellRSI, sellEMADist, takeProfit, stopLoss, buysOpened, buysTakeProfit, buysStopLoss, buysClosed, buyPoints, sellsOpened, sellsTakeProfit, sellsStopLoss, sellsClosed, sellPoints, totalTrades, totalWins, totalLosses, totalLosses, totalPoints):
		self.caseID = caseID
		self.weekendTimeout = weekendTimeout
		self.maxSpread = maxSpread
		self.activityCriterion = activityCriterion
		self.invertEMA = invertEMA
		self.buyRSI = buyRSI
		self.buyEMADist = buyEMADist
		self.sellRSI = sellRSI
		self.sellEMADist = sellEMADist
		self.takeProfit = takeProfit
		self.stopLoss = stopLoss
		self.buysOpened = buysOpened
		self.buysTakeProfit = buysTakeProfit
		self.buysStopLoss = buysStopLoss
		self.buysClosed = buysClosed
		self.buyPoints = buyPoints
		self.sellsOpened = sellsOpened
		self.sellsTakeProfit = sellsTakeProfit
		self.sellsStopLoss = sellsStopLoss	
		self.sellsClosed = sellsClosed
		self.sellPoints = sellPoints
		self.totalTrades = totalTrades
		self.totalWins = totalWins
		self.totalLosses = totalLosses
		self.totalLosses = totalLosses
		self.totalPoints = totalPoints
